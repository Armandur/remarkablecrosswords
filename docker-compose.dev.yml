services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - '8001:8001'
    env_file: .env
    environment:
      - ENABLE_SCHEDULER=false
    command: bash -c "uvicorn app.main:app --host 0.0.0.0 --port 8001 --reload 2>&1 | tee dev.log"
