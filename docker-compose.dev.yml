services:
  app:
    build: .
    volumes:
      - .:/app
    ports:
      - '8000:8000'
    env_file: .env
    environment:
      - ENABLE_SCHEDULER=false
    command: bash -c "uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload 2>&1 | tee dev.log"
